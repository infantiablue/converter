[supervisord]
logfile = %(ENV_SUPERVISOR_PWD)s/logs/supervisord.log
logfile_maxbytes = 50MB
logfile_backups=10
loglevel = info
pidfile = /tmp/supervisord.pid

[supervisorctl]

[inet_http_server]
port = 127.0.0.1:9001

[rpcinterface:supervisor]
supervisor.rpcinterface_factory = supervisor.rpcinterface:make_main_rpcinterface

[program:converter]
command=%(ENV_SUPERVISOR_PWD)s/env/bin/gunicorn wsgi:app --bind unix:/tmp/converter.sock
directory=%(ENV_SUPERVISOR_PWD)s
environment=PATH=%(ENV_SUPERVISOR_PWD)s/env/bin
stderr_logfile=%(ENV_SUPERVISOR_PWD)s/logs/gunicorn.err.log
autorestart=true
redirect_stderr=true